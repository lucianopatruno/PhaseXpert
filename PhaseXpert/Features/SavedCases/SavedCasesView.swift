import SwiftUI

struct SavedCasesView: View {
    var body: some View {
        NavigationStack {
            FeaturePlaceholder(
                title: "Saved Cases",
                systemImage: "tray.full",
                message: "Versioned SwiftData persistence arrives after the scientific provider contract is stable."
            )
        }
    }
}

